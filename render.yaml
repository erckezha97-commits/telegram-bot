services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    pythonVersion: 3.10
    envVars:
      - key: BOT_TOKEN
        sync: false
